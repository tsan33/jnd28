# 快速启动指南

## 5分钟快速上手

### 1. 安装依赖（首次运行）

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

### 3. 访问应用

打开浏览器访问: http://localhost:3000

### 4. 登录系统

使用任意用户名和密码登录（演示模式）

---

## 主要功能快速导航

### 📊 投注明细
- 路径: `/bets`
- 功能: 查询投注记录、筛选、导出CSV

### 📈 日统计
- 路径: `/stats`
- 功能: 查看统计数据、趋势图表

### ⚙️ 群与赔率
- 路径: `/odds`
- 功能: 管理群组赔率配置

---

## 常用命令

```bash
# 开发模式
npm run dev

# 生产构建
npm run build

# 预览生产构建
npm run preview

# 代码检查
npm run lint
```

---

## 目录结构一览

```
src/
├── components/     # 公共组件
├── pages/          # 页面组件
├── services/       # API服务
├── store/          # 状态管理
├── utils/          # 工具函数
├── App.jsx         # 根组件
└── main.jsx        # 入口文件
```

---

## 重要配置文件

- `vite.config.js` - Vite构建配置
- `package.json` - 项目依赖
- `API.md` - 后端接口文档
- `DEPLOYMENT.md` - 部署指南

---

## 开发提示

### 修改API地址

编辑 `vite.config.js`:

```javascript
server: {
  proxy: {
    '/user': {
      target: 'http://your-api-server:port',  // 改这里
      changeOrigin: true,
    },
  },
}
```

### 修改时区

编辑 `src/utils/format.js`:

```javascript
export const DEFAULT_TIMEZONE = 'Europe/Istanbul';  // 改这里
```

### 添加新页面

1. 在 `src/pages/` 创建新组件
2. 在 `src/App.jsx` 添加路由
3. 在 `src/components/Layout/MainLayout.jsx` 添加菜单项

---

## 常见问题

**Q: 端口被占用？**

A: 修改 `vite.config.js` 中的 `server.port`

**Q: API请求失败？**

A: 检查后端服务是否运行，检查代理配置是否正确

**Q: 页面空白？**

A: 打开浏览器控制台查看错误信息

---

## 获取帮助

- 📖 完整文档: [README.md](./README.md)
- 🔌 API文档: [API.md](./API.md)
- 🚀 部署指南: [DEPLOYMENT.md](./DEPLOYMENT.md)

---

**祝您使用愉快！** 🎉

